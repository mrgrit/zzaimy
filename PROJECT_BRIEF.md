# 프로젝트 브리프 v2 — 「짜이미」 국고사업 계획서 작성 지원 시스템

> 이 문서는 Claude Code(이하 CC)에게 전달하는 프로젝트 지시문이다.
> CC는 이 문서를 프로젝트 루트에 `PROJECT_BRIEF.md`로 두고, 모든 판단의 기준 문서로 삼는다.

---

## 프로젝트명과 명명 체계

**「짜이미」 / `ZZAIMY`**

「짜임」 + 「이」. 받침 있는 이름에 '-이'를 붙이는 한국어 애칭 형성 규칙을 따른 것이며(짜임 + 이 → [짜이미]), 이때의 Y는 영남이공대학교의 이니셜을 겸한다.

### 이름의 의미 — 설계 원칙과 직결되므로 반드시 이해할 것

- **짜다는 없는 것을 지어내는 것이 아니라 있는 실을 엮는 것이다.** 이 시스템은 문장을 창조하지 않고 이미 존재하는 근거를 엮어 배치한다. 5.3항(수치는 생성하지 않고 인출한다)이 이름 자체에 들어 있다.
- **날실은 공고**(평가지표·배점·목차 골격), **씨실은 기관의 실적**(성과 수치·보유 자산)이다. 계획서의 모든 문장은 이 둘이 교차하는 지점에서 생긴다.
- **짜임새**는 계획서의 승패를 가르는 지점이다. 배점 대조 검증기(6장)가 곧 짜임새 점검이다.

### 표기 규칙

| 용도 | 표기 |
|---|---|
| 공식 명칭 (보고서·발표) | 「짜이미」 / `ZZAIMY` |
| 저장소 | `zzaimy` |
| 모델명 | `ZZAIMY-Embed` / `ZZAIMY-Rerank` / `ZZAIMY-Writer` / `ZZAIMY-Extract` |
| 슬로건 | 공고를 날실로, 우리의 실적을 씨실로. |

`ZZAIMY`, `zzaimy` 외의 표기(`Y-ZZAIM`, `ZAIM`, `ZZAIMI` 등)는 사용하지 않는다.

---

## 0. CC에게 — 이 문서의 사용법

- **지금 당장 코드를 쓰지 마라.** 1단계는 리서치, 2단계는 설계다. 구현은 설계가 승인된 후에 시작한다.
- 이 문서에 적힌 **기술 후보 이름들은 "출발점"이지 "정답"이 아니다.** 작성 시점 기준의 목록이므로, GitHub와 웹에서 현재 상태를 직접 확인하고 더 나은 대안이 있으면 반드시 제안하라. **기억에 의존한 라이브러리 정보는 낡았을 수 있다고 가정하라.**
- 확신이 서지 않는 지점은 추측으로 채우지 말고 **질문하라.** 특히 문서 실물 구조, 계획서 양식의 일관성, 조직 운영 방식은 사용자만 아는 정보다.
- 모든 산출물은 **한국어로** 작성한다.

---

## 1. 배경과 문제

영남이공대학교에 국고사업(정부 재정지원 사업) 관련 문서가 수만 장 규모의 PDF로 축적되어 있다. 현재 이 자산은 파일 서버에 쌓여만 있고, 새 공고가 나올 때마다 담당자가 과거 문서를 수동으로 뒤져가며 사업계획서를 처음부터 다시 쓰고 있다.

### 보유 문서의 구성 (네 가지 계열)

| 계열 | 내용 | 시스템에서의 역할 |
|---|---|---|
| **① 공고·지침류** | 사업 공고문, 운영 지침, 평가 기준표 | 요구사항 정의 — 계획서 뼈대와 배점 체계 결정 |
| **② 사업계획서** | 과거 제출한 사업계획서 (선정/미선정 포함) | 논리 구조·표현 자산 **+ 생성 모델 학습 데이터** |
| **③ 프로그램 계획서/결과보고서** | 사업 운영 중 진행한 개별 프로그램의 계획과 성과 | **실적 데이터** — 정량 성과 수치의 원천. 최우선 가치 |
| **④ 지출 문서** | 기자재 구매, 환경개선, 재료비 등 집행 문서 | 단가 레퍼런스 — 예산 편성 근거. 우선순위 낮음 |

### 핵심 인식 — 보유 문서는 그 자체로 지도학습 데이터셋이다

이것이 이 프로젝트의 가장 중요한 전제다.

- **② 사업계획서** = "요구사항 + 근거자료 → 완성된 섹션"의 정답 쌍
- **③ 결과보고서** = "PDF → 정형 실적 데이터"의 정답 쌍
- 전체 문서 = 합성 질의 생성을 통한 "질문 → 정답 청크" 검색 학습 쌍

즉 **사람의 수작업 라벨링 없이 모델 학습 데이터를 확보할 수 있다.** 설계 전반에서 이 자산을 활용하는 방향을 우선한다.

---

## 2. 목표 — 두 개의 축

이 프로젝트는 두 가지를 동시에 달성해야 하며, **둘 다 필수**다.

### 축 A — 실용 시스템

> 새 국고사업 공고문(PDF)을 입력하고 방향성 몇 가지를 지정하면, 기관의 실제 실적과 자산에 근거한 사업계획서 초안을 생성한다.

**성공 기준**
- 생성 초안이 공고의 **평가지표·배점 항목을 빠짐없이 반영**한다.
- 모든 정량 수치가 실제 보유 문서에서 인출되었으며 **출처(문서명 + 페이지)가 추적 가능**하다.
- 담당자의 초안 작성 시간이 **2주 → 2~3일**로 단축된다.

**목표가 아닌 것**: 사람 검수 없는 제출 완성본 생성. 목표는 **"70% 초안"** 이다.

### 축 B — 도메인 특화 모델 개발

> 오픈웨이트 모델을 베이스로, 국고사업 문서 도메인에 특화된 모델을 학습·구축하고 베이스 대비 개선폭을 정량 입증한다.

외부 데이터소스 구축(RAG 인덱스)만으로는 성과 요건을 충족하지 못한다. **학습된 가중치를 산출물로 확보해야 한다.** 단, 이는 형식적 요건 충족이 아니라 **실제로 시스템 성능을 올리는 방식**으로 수행한다. 구체적 대상은 7장 참조.

**성공 기준**
- 베이스 모델 대비 정량 개선폭이 재현 가능하게 측정된다.
- 모델 카드, 평가 보고서, 버전 이력을 갖춘 산출물 세트가 존재한다.

---

## 3. 최종 산출물 정의

프로젝트 종료 시 아래가 존재해야 한다.

**시스템**
1. 문서 파싱·정제 파이프라인 (④ 계열 제외)
2. 실적 카드 DB (정형 테이블)
3. 하이브리드 검색 인덱스
4. 섹션별 계획서 생성 파이프라인 + 배점 대조 검증기

**모델 (가중치 산출물)**
5. 도메인 특화 임베딩 모델 — `ZZAIMY-Embed-v1` (명명은 라이선스 확인 후 확정)
6. 도메인 특화 리랭커 — `ZZAIMY-Rerank-v1`
7. 계획서 섹션 생성 모델 (LoRA) — `ZZAIMY-Writer-v1`
8. 실적 추출 소형 모델 — `ZZAIMY-Extract-v1`

**문서**
9. 모델별 모델 카드 (베이스 모델, 학습 데이터 규모·출처, 학습 방식, **라이선스 고지**)
10. 평가 보고서 (베이스 대비 개선폭, 평가셋 구성, 재현 절차)
11. 운영 서버 요구사양 산정 보고서

---

## 4. 제약조건 및 환경

### 하드웨어
- **개발/프로토타입**: NVIDIA DGX Spark 1대 (128GB 통합 메모리)
- **배치 처리/학습**: DGX Spark 최대 20대 (ConnectX-7 200Gb/s)
- **실습실**: RTX 5070 데스크톱 약 25대 (12GB VRAM, 시스템 메모리 32GB) — 본 프로젝트 주 대상 아님
- **운영 서버**: **미구매.** Phase 1~4 결과로 요구사양을 산정한 뒤 결정한다. 지금 사양을 가정하지 마라.

### DGX Spark의 구조적 특성 — 설계에 반드시 반영할 것

1. **메모리 대역폭이 병목이다.** LPDDR5X 기반으로 약 273GB/s 수준이라 HBM 기반 데이터센터 GPU 대비 현저히 낮다. 토큰 생성 속도가 대역폭에 거의 정비례하므로 **dense 대형 모델은 체감 속도가 느리다.** → **MoE 아키텍처 모델을 우선 평가하라.** 토큰당 활성 파라미터가 작아 대역폭 제약 환경에서 유리하다.
2. **프리필과 배치 처리는 연산 위주라 강점이다.** 문서 파싱, 임베딩 생성, 실적 추출, 모델 학습 등 이 프로젝트 작업량의 대부분이 여기 해당한다.
3. **파싱·추출·임베딩 배치 작업은 문서 단위로 완전 병렬화된다.** Spark 20대를 클러스터로 묶을 필요 없이 워크로드를 20등분해 독립 워커로 던지면 된다. 분산 추론 클러스터링을 시도하지 마라.
4. **aarch64(ARM64) 환경이다.** Grace CPU 기반이므로 x86 전용 휠·Docker 이미지는 동작하지 않는다. **특히 OCR/문서 파싱 계열에 ARM 미지원 프로젝트가 많다.**

### 필수 제약
- **온프레미스 전제.** 문서에 개인정보와 기관 내부 정보가 포함되어 외부 API로의 문서 전송은 원칙적으로 불가. 로컬 추론 가능한 오픈웨이트 모델을 기본으로 설계한다.
- **한국어 문서 100%.** 모든 구성요소(OCR, 임베딩, 형태소 분석, 리랭커, 생성 모델)는 한국어 성능 기준으로 선정한다. 영어 벤치마크는 참고 자료일 뿐이다.
- **교내 전용.** 대외 서비스나 상용화 계획 없음. 실용성과 운영 단순성을 우선한다.
- **라이선스 검토 필수.** 공공기관 사업 관련 시스템이므로 AGPL·상용 제한·비상업 조항이 있는 프로젝트는 채택 전 반드시 위험으로 보고한다.

### 베이스 모델 라이선스와 명명권 (축 B에 직결)

- **Llama 계열**: 파생 모델명에 "Llama"를 포함하고 "Built with Llama"를 표기할 의무가 있다. 독자 명명 불가.
- **Gemma 계열**: 유사한 고지 조건이 있다.
- **Apache 2.0 계열(Qwen, Mistral 등)**: 그러한 제약이 없어 독자 명명이 자유롭다.

→ **베이스 모델 선정 시 한국어 성능과 함께 명명 자유도를 평가 항목에 포함하라.** 산출물명을 `ZZAIMY-*`로 가져가려면 이 조건이 만족되어야 한다.

---

## 5. 설계 원칙

### 5.1 바닥부터 만들지 않는다
문서 파싱, 청킹, 벡터 검색, 하이브리드 랭킹, 리랭킹, 학습 프레임워크, 평가 하네스 — 모두 성숙한 오픈소스가 존재한다. **직접 구현하는 것은 도메인 고유 로직뿐이다.**

- 조립할 것: 파서, 벡터DB, 검색 프레임워크, 서빙 엔진, 학습 프레임워크, 평가 툴
- 직접 만들 것: 공고→요구사항 스키마 변환, 실적 카드 추출, 섹션별 생성 파이프라인, 배점 대조 검증기, 학습 데이터 생성 로직

### 5.2 파인튜닝의 역할을 정확히 한정한다

**이것이 이 프로젝트에서 가장 오해하기 쉬운 지점이다.**

- **금지**: 문서를 학습시켜 모델에 **지식을 주입**하려는 시도. 사실 신뢰성이 확보되지 않고, 출처 인용이 불가능하며, 문서 개정을 따라갈 수 없다.
- **필수**: **검색 성능 개선**(임베딩·리랭커), **문체·구조 학습**(섹션 생성), **추출 정확도 향상**(실적 카드).

분업 원칙: **"무엇을 쓸 것인가"는 검색이 담당하고, "어떻게 쓸 것인가"는 파인튜닝이 담당한다.** 이 경계를 흐리면 즉시 수치 환각이 발생한다.

### 5.3 숫자는 생성하지 않고 인출한다
계획서에 없던 실적을 모델이 지어내면 이는 단순 오류가 아니라 **허위 기재로 사업 취소·환수·제재 사유**가 된다.

- 정량 수치 자리는 **반드시 출처 문서 참조와 함께** 채워진다.
- 근거를 못 찾으면 임의로 채우지 말고 **빈칸으로 남기고 사용자에게 표시**한다.
- 프롬프트가 아니라 **파이프라인 구조 수준에서 강제**한다. 생성 모델이 학습으로 특정 수치를 외우게 두지 않는다.

### 5.4 통짜 생성을 시도하지 않는다
한 번의 LLM 호출로 계획서 전체를 뽑는 구조는 반드시 실패한다. **섹션 단위 루프**로 설계한다.

### 5.5 평가셋이 먼저다 — 축 B의 전제이기도 하다
베이스라인 없이 파인튜닝하면 **개선을 주장할 수 없다.** 축 B의 성과 입증은 전적으로 평가 체계에 의존한다. 따라서 평가셋 구축은 선택이 아니라 축 B의 선행 조건이다.

### 5.6 문서 4계열의 처리 경로를 분리한다
같은 PDF지만 역할이 전혀 다르다. 단일 인덱스에 통합하지 마라.

---

## 6. 시스템 아키텍처 개요

### 생성 루프

```
1. 공고 파싱      → 목차 골격 + 평가지표 + 배점 + 분량 제약 + 예산 기준을 정형 스키마로 추출
2. 사용자 입력    → 핵심 컨셉, 차별화 포인트, 예산 규모 ("방향 몇 가지")
3. 섹션별 루프    → 각 섹션마다:
                     a. 필요한 근거 인출 (실적 카드 DB + 보유 자산 + 과거 표현)
                     b. 섹션 생성 모델로 초안 작성
                     c. 인출된 수치만 사용했는지 검증
4. 배점 대조 검증  → 배점 항목별로 본문 반영 여부를 기계적으로 체크, 누락 보고
5. 예산표 생성    → LLM 생성 금지. 단가 DB + 계산 로직으로 산출
6. 출력           → 섹션별 출처 주석이 붙은 초안
```

4번은 이 시스템의 차별화 지점이다. 사람이 계획서를 쓸 때 가장 흔히 놓치는 것이 배점 항목 누락인데, 이는 기계가 훨씬 잘한다.

### 실적 카드 DB — 최우선 구축 대상

결과보고서에서 추출하는 정형 테이블. 이것이 완성되면 계획서 작성의 절반이 끝난다.

```
사업명 | 연도 | 예산규모 | 참여인원 | 정량성과 | 소관부서 | 활용장비 | 출처문서 | 출처페이지
```

이는 벡터 검색 문제가 아니라 **구조화 추출(structured extraction)** 문제다. 문서당 LLM으로 JSON을 뽑아 DB에 적재하는 배치 작업이며, 산출물 8번(`ZZAIMY-Extract`)의 대상이다.

### 지식 그래프의 범위

**풀스케일 GraphRAG는 기본 채택하지 않는다.** 비용 대비 효과를 입증한 경우에만 도입한다. 이 프로젝트에서 그래프가 필요한 관계는 좁다:

`사업 ↔ 프로그램 ↔ 성과지표 ↔ 예산항목 ↔ 장비`

실적 카드 DB가 정형화되면 이 관계는 자연스럽게 잡힌다. **정형 DB + 좁은 그래프로 충분한지**를 먼저 판단하고 근거와 함께 보고하라.

---

## 7. 모델 개발 트랙 (축 B 상세)

네 개 모델을 개발한다. **모두 실제 시스템 성능 개선에 기여해야 하며, 성과 요건만을 위한 학습은 수행하지 않는다.**

### 7.1 `ZZAIMY-Embed` — 도메인 특화 임베딩 모델

- **왜 필요한가**: RAG에서 검색이 틀리면 생성 품질은 무의미하다. 범용 임베딩은 "학사운영지원", "교육과정 개편", "성과관리 지표" 같은 교내·국고사업 용어의 관계를 모른다. **ROI가 가장 높은 학습 대상이다.**
- **학습 데이터**: 보유 문서 청크마다 LLM으로 합성 질의를 생성해 (질의, 정답 청크) 쌍을 수만 개 구축. **수작업 라벨링 불필요.** 하드 네거티브 샘플링 전략을 함께 설계하라.
- **학습 방식**: 대조학습(contrastive). 베이스 후보는 8.1 D항 참조.
- **평가 지표**: Recall@k, MRR, nDCG — 베이스 모델 대비

### 7.2 `ZZAIMY-Rerank` — 도메인 특화 리랭커

- 7.1과 동일한 데이터 자산을 재활용한다. 검색 품질 개선 효과가 크므로 7.1과 세트로 진행한다.
- **평가 지표**: 리랭킹 후 Recall@k, 최종 응답 정확도 기여분

### 7.3 `ZZAIMY-Writer` — 계획서 섹션 생성 모델 (LoRA SFT)

축 B의 본체이며 "모델을 만들었다"에 가장 직접적으로 대응한다.

- **학습 데이터 구성**
  ```
  입력: 공고에서 추출한 해당 섹션 요구사항 + 평가지표
        + 인출된 근거자료(실적 수치, 보유 자산)
        + 방향성 지시
  출력: 실제 제출했던 계획서의 해당 섹션 원문
  ```
  과거 계획서 20~30건을 섹션 단위로 해체하면 수백~수천 건의 학습 쌍이 나온다.
- **학습되는 것**: 국고사업 계획서 특유의 문체, 논리 전개 순서, "필요성 → 현황 → 전략 → 기대효과" 서술 관습. 프롬프트 엔지니어링으로는 잘 되지 않는 영역이다.
- **절대 학습시키지 않을 것**: 실적 수치 자체. 수치는 항상 입력으로 주입한다. 학습 데이터 구성 시 **출력에 등장하는 모든 수치가 입력 근거자료에도 존재하도록** 데이터를 정제하라. 이 정제를 건너뛰면 모델이 수치를 외워서 지어내기 시작한다.
- **평가 지표**: 사람 평가(문체 적합성, 논리 완결성) + 평가지표 반영률 + 수치 환각률(반드시 0에 수렴해야 함)

### 7.4 `ZZAIMY-Extract` — 실적 추출 소형 모델

- **왜 필요한가**: 수만 장 배치 추출에 대형 모델을 쓰면 처리 시간이 감당되지 않는다. 3~7B급을 이 작업에 특화시키면 정확도를 유지하며 처리량이 몇 배가 된다. Spark 20대 병렬과 궁합이 좋다.
- **학습 데이터**: 대형 모델로 추출한 결과를 사람이 검수한 것을 정답으로 사용(distillation). 검수 대상은 수백 건 규모로 한정.
- **평가 지표**: 필드별 정확도, 대형 모델 대비 처리량 배수

### 7.5 산출물 형식

각 모델에 대해 다음을 갖춘다.
- 모델 카드 — 베이스 모델과 **라이선스 고지**, 학습 데이터 규모·출처·생성 방식, 하이퍼파라미터, 학습 비용(GPU-시간)
- 평가 보고서 — 베이스 대비 개선폭, 평가셋 구성, **재현 절차**
- 버전 관리 — `v0.1`, `v0.2` 형태의 반복 개선 이력

### 7.6 대외 표현 지침

- **사용 가능**: "국고사업 문서 도메인 특화 모델 개발", "오픈웨이트 기반 도메인 적응", "검색 정확도 N%p 개선"
- **사용 금지**: "자체 개발 LLM", "독자 파운데이션 모델", "from scratch 학습"

정확한 표현이 방어가 쉽고 실무적으로도 더 설득력 있다. 과장은 나머지 진짜 성과까지 의심받게 만든다.

---

## 8. Phase 0 — 리서치 (지금 수행할 작업)

### 8.1 조사 영역과 출발점 후보

아래는 검증되지 않은 출발점이다. GitHub에서 **최근 커밋 시점, 스타 수, 이슈 활성도, 라이선스, 한국어 지원, ARM64 지원**을 직접 확인하고, 목록에 없는 더 나은 대안을 적극적으로 찾아라.

**A. 문서 파싱 / OCR** — 전체 품질의 70%가 여기서 결정된다. **가장 깊게 조사할 것.**
- 후보: MinerU, Docling, marker, olmOCR, Surya, unstructured, PyMuPDF4LLM
- 평가 축: **표 구조 보존 능력**(결과보고서의 실적이 표 안에 있음), 한국어 스캔본 정확도, 문서 계층 추출, 배치 처리 성능, **aarch64 동작 여부**

**B. RAG 프레임워크 / 플랫폼**
- 후보: LlamaIndex, Haystack, RAGFlow, R2R, txtai, Dify
- 판단 포인트: 통합 플랫폼 vs 라이브러리 조립. 이 프로젝트는 **커스텀 생성 로직과 자체 학습 모델 투입이 무겁기 때문에**, 통합 플랫폼의 제약(커스텀 모델 교체 가능 여부, 파이프라인 개입 지점)을 반드시 평가하라.

**C. 지식 그래프**
- 후보: LightRAG, Microsoft GraphRAG, nano-graphrag, Cognee
- 6장의 범위 한정을 전제로, 도입 필요성 자체를 판단해 보고하라.

**D. 임베딩 / 리랭커 (한국어) — 7.1·7.2의 베이스**
- 후보: BGE-M3, Qwen3-Embedding, KURE, KoE5, bge-reranker-v2-m3, Qwen3-Reranker
- 한국어 검색 벤치마크 결과를 근거로 제시하라.
- **파인튜닝 용이성**(공식 학습 스크립트 제공 여부, 커뮤니티 사례)을 반드시 평가 항목에 포함하라.

**E. 임베딩·리랭커 학습 프레임워크**
- 후보: sentence-transformers, FlagEmbedding, SWIFT
- 하드 네거티브 마이닝 지원 여부가 중요하다.

**F. LLM 파인튜닝 프레임워크 (7.3·7.4용)**
- 후보: LLaMA-Factory, Axolotl, Unsloth, TRL, SWIFT
- **평가 축: aarch64 + Blackwell 환경에서의 실제 동작 여부.** 이 조건에서 걸러지는 후보가 많을 것으로 예상되므로 반드시 확인하라.

**G. 하이브리드 검색 / 한국어 형태소**
- 후보: Kiwi, mecab-ko, rank_bm25, OpenSearch Nori 분석기
- 행정 문서는 정확한 용어 매칭이 중요해 BM25 비중이 높을 것으로 예상된다.

**H. 벡터 DB**
- 후보: Qdrant, Milvus, Weaviate, pgvector, LanceDB
- 평가 축: **메타데이터 필터링 성능**(연도·사업명·문서종류 필터가 핵심), 온프렘 운영 난이도, aarch64 지원

**I. 구조화 추출**
- 후보: Instructor, Outlines, LangExtract, BAML, vLLM guided decoding
- 스키마 강제(constrained decoding) 품질이 핵심

**J. LLM 서빙**
- 후보: vLLM, SGLang, Ollama, llama.cpp
- **DGX Spark(aarch64 + Blackwell, 통합 메모리)에서의 실제 지원 상태를 반드시 확인하라.** LoRA 어댑터 동적 로딩 지원 여부도 확인 대상이다.

**K. 생성 모델 (오픈웨이트, 한국어)**
- 후보: Qwen3 계열, EXAONE, A.X, gpt-oss, Llama 계열
- 평가 축: 한국어 공문서 문체, 긴 컨텍스트, **MoE 여부(4장 참조 — 대역폭 제약상 우선)**, 파인튜닝 생태계 성숙도, **라이선스와 명명 자유도(4장 참조)**

**L. 평가 프레임워크**
- 후보: Ragas, DeepEval, TruLens, promptfoo, AutoRAG
- 한국어 지원과 커스텀 지표 정의 가능성. **축 B의 성과 입증이 여기 달려 있으므로 가볍게 넘기지 마라.**

**M. 개인정보(PII) 탐지·마스킹**
- 후보: Microsoft Presidio, 한국어 PII 탐지 오픈소스
- **한국 특화 패턴**(주민등록번호, 계좌번호, 사업자등록번호, 학번, 국내 휴대전화 형식) 지원 여부가 관건. 미지원이면 커스텀 룰 개발 범위를 산정하라.

**N. 유사 선행 사례**
- "RFP 자동 응답", "proposal generation", "제안서 자동 작성", "grant writing AI" 등으로 오픈소스와 논문을 조사하라. 바퀴를 다시 발명하지 않기 위함이다.

### 8.2 각 후보에 대해 기록할 항목

- 저장소 URL, 스타 수, 최근 커밋 시점, 릴리스 주기
- **라이선스** (상업/공공 활용 제약 명시)
- **aarch64(ARM64) 네이티브 지원 여부** — 공식 휠/이미지 제공 여부를 확인. 미확인이면 "미확인"으로 표기
- 한국어 지원 수준 (문서화된 근거 우선)
- 온프렘 실행 가능 여부 및 GPU 요구사항
- 이 프로젝트에서의 적합성과 **구체적 위험 요인**

### 8.3 Phase 0 산출물

`research/` 디렉터리에 아래를 생성한다.

1. `research/00-summary.md` — 영역별 **최종 추천 1안 + 대안 1안**, 선정 근거와 위험. 이 문서만 읽어도 의사결정이 가능해야 한다.
2. `research/01-{영역명}.md` — 영역별 상세 비교표
3. `research/50-model-track.md` — 축 B 전용. 4개 모델 각각의 베이스 모델 후보, 학습 프레임워크, 데이터 생성 방안, 예상 학습 비용(GPU-시간), 평가 지표 정의
4. `research/99-open-questions.md` — 사용자에게 물어야 할 질문 목록

**중요**: 추천에는 반드시 **탈락 이유**도 함께 적어라. "왜 이걸 안 골랐는가"가 나중에 더 유용하다.

---

## 9. Phase 1 — 아키텍처 설계 및 실행 계획

Phase 0 산출물을 사용자가 검토·승인한 후 진행한다.

1. `docs/architecture.md` — 전체 데이터 흐름, 문서 4계열별 처리 경로 분리, 실적 카드 DB 스키마, 섹션별 생성 파이프라인, 배점 대조 검증기, 예산표 계산 로직
2. `docs/model-plan.md` — 4개 모델의 학습 데이터 생성 파이프라인 상세, 학습 일정, 평가 프로토콜, **수치 환각 방지를 위한 학습 데이터 정제 규칙**
3. `docs/eval-plan.md` — 평가셋 구축 방안(문항 수, 정답 기준, 사람 검수 절차), 검색·생성 각각의 지표 정의, 베이스라인 측정 절차
4. `docs/pilot-plan.md` — **파일럿 범위**: 최근 3~5년 유사 유형 사업의 계획서 20~30건 + 대응 결과보고서. 전체 수만 장으로 시작하지 않는다. 마일스톤과 단계별 성공 기준
5. `docs/risks.md` — 개인정보 처리(인덱싱 **이전** 단계 PII 스크리닝. 사후 조치는 벡터DB 전체 재구축을 의미), 접근권한 설계(문서별 열람 등급을 검색 단계에서 필터링 — 나중에 붙이기 매우 어려움), 수치 환각 방지 구현, 라이선스 위험

---

## 10. 이후 로드맵 (참고용, 지금 실행하지 않음)

- **Phase 2**: 파싱 파이프라인 구축, 파일럿 문서 처리, **평가셋 구축**
- **Phase 3**: 검색 파이프라인 + 실적 카드 DB 구축 → **베이스 모델로 베이스라인 측정**(축 B 성과의 비교 기준)
- **Phase 4**: 섹션별 생성 파이프라인 + 배점 검증기, 베이스라인 대비 시스템 성능 확정
- **Phase 5**: **모델 개발 트랙 실행** — 7.1~7.4 순서로 학습·평가·개선폭 측정
- **Phase 6**: 필요 시 그래프 계층 추가 후 A/B 측정 (실제 개선이 확인될 때만 채택)
- **Phase 7**: 전체 문서 확장, ④ 지출 문서 계열 편입, 운영 서버 사양 확정 및 이관

**Phase 3의 베이스라인 측정을 건너뛰면 축 B의 성과를 입증할 수 없다.** 순서를 바꾸지 마라.

---

## 11. 금지사항

- Phase 0/1 단계에서 구현 코드를 작성하는 것
- 검증하지 않은 라이브러리 이름·성능 수치를 사실로 기술하는 것 (**출처가 없으면 "미확인"으로 표기**)
- 문서 학습으로 **지식을 주입**하는 설계를 제안하는 것 (5.2의 허용 범위와 혼동하지 말 것)
- 생성 모델 학습 데이터에 입력 근거 없는 정량 수치가 포함되도록 두는 것
- 베이스라인 측정 없이 파인튜닝을 먼저 수행하는 것
- ④ 지출 문서 계열을 초기 범위에 포함시키는 것
- Spark 여러 대를 분산 추론 클러스터로 묶으려는 시도 (4장 3항 참조)
- 운영 서버 사양을 가정하고 설계하는 것
- 사용자만 알 수 있는 정보(문서 실물 구조, 조직 운영 방식)를 추측으로 채우는 것

---

## 12. 작업 방식

- Git 저장소로 관리하고 리서치 문서도 커밋한다.
- 웹 검색과 GitHub 조회를 적극 사용한다.
- 각 Phase 종료 시 사용자에게 요약 보고하고 승인을 받은 뒤 다음 단계로 넘어간다.
- 판단이 갈리는 지점은 임의 결정하지 말고 선택지와 트레이드오프를 제시하라.
- 학습 관련 결정(베이스 모델, 하이퍼파라미터, 데이터 규모)은 반드시 근거와 함께 기록한다. 모델 카드의 재료가 된다.

---

## 13. 지금 시작할 것

1. 저장소를 초기화하고 이 문서를 `PROJECT_BRIEF.md`로 커밋한다.
2. `research/` 디렉터리를 만든다.
3. **8.1의 A(문서 파싱)부터** 조사를 시작한다. 전체 품질을 좌우하는 영역이므로 가장 깊게 판다. **aarch64 지원 여부를 1차 필터로 적용하라** — Spark에서 돌지 않는 도구는 후보가 될 수 없다.
4. A 완료 후 **D(임베딩·리랭커)와 K(생성 모델)** 로 넘어간다. 축 B의 베이스 모델 선정이 걸려 있어 조기에 방향을 잡아야 한다.
5. 조사 중 사용자 확인이 필요한 사항은 `research/99-open-questions.md`에 누적하고 Phase 0 보고 시 함께 제시한다.
